{
  "name": "poc-embedded-messenger",
  "version": "1.0.0",
  "description": "Embedded Messenger",
  "scripts": {
    "init": "npm --prefix ./sample-app i && npm --prefix ./textus-embedded i && npm run link",
    "link": "cd ./textus-embedded && npm link && cd ../sample-app && npm link @textus/embedded",
    "sample": "npm --prefix ./textus-embedded run build && npm --prefix ./sample-app run dev",
    "watch": "npm --prefix ./textus-embedded run build:watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/TextUsBiz/POC-Embedded-Messenger.git"
  },
  "author": "John Brown (john.brown@textus.com)",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/TextUsBiz/POC-Embedded-Messenger/issues"
  },
  "homepage": "https://github.com/TextUsBiz/POC-Embedded-Messenger#readme",
  "devDependencies": {
    "jsdoc": "^4.0.2"
  },
  "dependencies": {
    "live-server": "^1.2.2"
  }
}